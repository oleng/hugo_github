version: '3'
services:

    server:
        image: klakegg/hugo:0.65.3-ext-ubuntu
        command: server --verbose --verboseLog --gc -t lithiumx
        volumes:
            - ./:/src
            - ./public:/target
        ports:
            - 1313:1313
        container_name: hugo

